import 'package:flutter/material.dart';

class RandomsGameScreen extends StatelessWidget {
  const RandomsGameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Random's Game Screen"),
      ),
    );
  }
}
